import dotenv from 'dotenv';

// Cargar las variables desde el archivo prod.env
dotenv.config({ path: '.env.prod' });

/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
